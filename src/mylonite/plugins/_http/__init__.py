"""Plain HTTP-agent target adapter (``transport: rest``).

Lets Mylonite drive any HTTP agent by describing its request shape in a
``target.yaml`` ``request`` block — no MCP wrapper, no changes to the app under
test. See :mod:`mylonite.plugins._http.http_adapter`.
"""
