"""Reference implementations of the five extension-point contracts.

These exist so that ``pip install mylonite`` exercises the plugin discovery
pathway end-to-end on day one and so the contracts have a small, canonical
"this is what an implementation looks like" worked example. They are
deliberately minimal — production users replace them with real plugins.
"""
