**Weakness class: excessive agency (W4).** A consequential action (e.g. sending
an email, moving funds) executed without confirmation. Attenuate tool
permissions and require an explicit human-approval / two-step confirm before any
irreversible action; stage rather than send. The guarded reference twin splits
`send_email` into a stage step plus a separate `confirm_send`.
