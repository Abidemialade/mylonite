**Weakness class: unrestricted egress / SSRF (W3).** The agent fetched an
attacker-controlled URL. Enforce a hostname/domain allowlist on any
network-egress tool, reject private/loopback/metadata addresses, and fail closed
on anything off the list. The guarded reference twin enforces a fetch allowlist
and refuses out-of-list hosts with an error.
