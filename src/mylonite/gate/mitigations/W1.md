**Weakness class: tool-description instruction smuggling (W1).** A tool or
function *description* carried instructions the model obeyed. Treat tool
metadata as untrusted: strip or escape directives in descriptions, restrict
descriptions to a strict character allowlist, and never let schema text alter
the agent's policy. The guarded reference twin validates descriptions against an
ASCII allowlist before exposing them.
