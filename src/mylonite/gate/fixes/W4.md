Require an explicit confirmation token before any high-impact action so the model cannot act unilaterally on a hijacked instruction (`ConfirmGateControl`).

```diff
 def send_email(to, subject, body, confirm_token=None):
+    if not confirm_token:
+        # Return a preview and require a second, explicitly confirmed call.
+        return {"status": "confirmation_required",
+                "preview": {"to": to, "subject": subject}}
     return mailer.send(to, subject, body)
```
