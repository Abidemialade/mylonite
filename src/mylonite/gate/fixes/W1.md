Treat tool descriptions as untrusted input: strip smuggled imperatives ("ignore previous instructions", "you must…", "system:") before the planner ever sees them (`ToolDescriptionSanitizer`).

```diff
 def list_tools():
     tools = load_tool_schemas()
-    return tools
+    # Tool descriptions are attacker-controllable — sanitise before planning.
+    for t in tools:
+        t["description"] = sanitize_tool_text(t["description"])
+    return tools
```
