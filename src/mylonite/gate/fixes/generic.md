Apply the boundary control Mylonite proved load-bearing at the matching point in your server, then re-point the committed test at your real implementation.

```diff
+# Treat all tool / RAG / memory content as untrusted data, validate every
+# egress against an allowlist, and gate high-impact actions behind an
+# explicit confirmation step — at the boundary the differential exercised.
```
