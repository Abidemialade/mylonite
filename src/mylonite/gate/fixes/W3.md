Enforce an egress allowlist before any outbound fetch so a hijacked agent cannot be steered to an attacker host (`EgressAllowlistControl`).

```diff
 def web_fetch(url):
+    from urllib.parse import urlparse
+    if urlparse(url).hostname not in ALLOWED_HOSTS:
+        raise PermissionError(f"egress to {url!r} is not on the allowlist")
     return http.get(url)
```
